{
  "buildCommand": "cd ../shared && bun run build && cd ../client && bun run build",
  "outputDirectory": "dist",
  "installCommand": "bun install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
