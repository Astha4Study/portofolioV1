{
  "version": 2,
  "installCommand": "cd .. && bun install && cd server",
  "buildCommand": "cd ../shared && bun run build && cd ../server && bunx prisma generate && bun run build",
  "framework": null,
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}